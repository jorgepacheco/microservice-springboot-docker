

Empaquetado de apllicacion y generación de la imagen
----------------------------------------------------

mvn clean package docker:build


Docker compose
--------------

$ docker-compose -f docker-compose.yml up -d


HA Proxy
--------


 docker run -d --name web1 springio/micro-service-spring-boot
 docker run -d --name web2 springio/micro-service-spring-boot

 docker run -d --name haproxy --link web1:web1 --link web2:web2  -p 80:80 tutum/haproxy



docker-compose -f docker-compose-v2.yml up

docker-compose -f docker-compose-v2.yml scale web=5